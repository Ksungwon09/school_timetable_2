<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/widgetContainer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#FFFFFFFF"
    android:padding="8dp">

    <TextView
        android:id="@+id/tvWidgetTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textSize="14sp"
        android:textStyle="bold"
        android:textColor="#000000"
        android:text="오늘의 시간표"
        android:layout_marginBottom="4dp" />

    <TextView
        android:id="@+id/tvWidgetEmpty"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:visibility="gone"
        android:text="설정된 시간표가 없습니다."
        android:textColor="#888888" />

    <LinearLayout
        android:id="@+id/layoutWidgetTimetable"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:weightSum="8" />

    <!-- We will programmatically add rows here in RemoteViews, but since RemoteViews has limitations,
         we will use a static layout with max 8 periods for simplicity and AutoSize compat -->

</LinearLayout>
